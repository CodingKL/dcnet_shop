$(function()
{
    // 积分使用切换
    $('.plugins-points-buy-container label input[type="checkbox"]').on('change', function()
    {
        var value = $(this).is(':checked') ? 1 : null;
        window.location.href = UrlFieldReplace('is_points', value);
    });
});